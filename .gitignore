node_modules/
dist/
build/
coverage/
*.log

# secrets — never commit
.env
.env.*
!.env.example
*.pem
*.key
profiles/

# runtime state
data/
*.jsonl
# except the published sample, which exists so the verifier can be tried offline
!examples/sample-decisions.jsonl
dist-site/
